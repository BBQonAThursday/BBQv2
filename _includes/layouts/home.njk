<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ renderData.title or title or metadata.title }}</title>
    <meta name="Description" content="{{ renderData.description or description or metadata.description }}">
    <link rel="stylesheet" href="{{ '/css/main.css' | url }}">
    <link rel="stylesheet" href="{{ '/css/prism-base16-monokai.dark.css' | url }}">
    <link rel="alternate" href="{{ metadata.feed.path | url }}" type="application/atom+xml" title="{{ metadata.title }}">
     <link href="https://fonts.googleapis.com/css?family=Saira:300,400,600,700&display=swap" rel="stylesheet"> 
  </head>
  <body class="home--page">
    <header class="home--nav">
      <h1 class="h1 site--heading"><a class="link" href="{{ '/' | url }}">{{ metadata.title }}</a></h1>

      {#- Read more about `eleventy-navigation` at https://www.11ty.dev/docs/plugins/navigation/ #}
      {% set projectlist = collections.projects %}
      {% set postslistCounter = collections.posts | length %}
      <article class="nav--container">
        <h2 class="h2 heading--year">2020</h2>
        {% include "projectlist.njk" %}
      </article>
    </header>
   
    <main class="home--intro">
      
      {{ content | safe }}
    </main>

  
  </body>
</html>
